<p id="notice"><%= notice %></p>

<p>
  <strong><%= User.human_attribute_name(:username) %>:</strong>
  <%= link_to User.find(@book.user_id).username, user_path(@book.user_id) %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:title) %>:</strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:memo) %>:</strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:author) %>:</strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:picture) %></strong>
  <%= image_tag @book.picture_url, size: '200x200' if @book.picture.present? %>
</p>

<% if @book.user_id == current_user.id %>
  <%= link_to t('helpers.select.Edit'), edit_book_path(@book) %> |
  <%= link_to t('helpers.select.Destroy'), @book, method: :delete, data: { confirm: t('messages.sure?') } %></td>
<% end %>
<br><%= link_to t('helpers.select.Back'), books_path %>
