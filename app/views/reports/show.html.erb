<h1><%= t('helpers.column.report_title', title: @report.title ) %></h1>
<p id="notice"><%= notice %></p>
<%= link_to t('helpers.select.back_to', page: "トップページ"), root_path %>
<hr>
<p>
  <strong><%= Report.human_attribute_name(:title) %>:</strong>
  <%= @report.title %>
</p>

<p>
  <strong><%= Report.human_attribute_name(:updated_at) %>:</strong>
  <%= l @report.updated_at, format: :short %>
</p>

<p>
  <strong><%= Report.human_attribute_name(:content) %>:</strong>
  <%= @report.content %>
</p>

<% if @report.user == current_user %>
  <%= link_to t('helpers.select.Edit'), edit_report_path(@report) %> |
  <%= link_to t('helpers.select.Destroy'), @report, method: :delete, data: { confirm: t('messages.sure?') } %></td>
<% end %>
<br><hr>
<%= render 'comments/index', comments: @comments %>
<%= render 'comments/form', comment: @comment %>
